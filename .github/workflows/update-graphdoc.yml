name: Generate and Update Graphdoc
on:
  create:
    branches:
      - 'update-graphdoc/*'
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{ secrets.GH_PAT }}
      - uses: borales/actions-yarn@v2.0.0
        with:
          cmd: install
      - run: |
          git config user.email "dev@vydia.com"
          git config user.name "Vydia"
          git checkout -B update_graphdoc
        with:
          cmd: generate-graphdoc
      - run: |
          git add .
          git commit -m "update graphdoc"
          git push origin update_graphdoc
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: devmasx/merge-branch@v1.4.0
        with:
          type: now
          from_branch: update_graphdoc
          target_branch: master
          github_token: ${{ github.token }}
